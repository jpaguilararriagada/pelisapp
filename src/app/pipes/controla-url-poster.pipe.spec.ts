import { ControlaUrlPosterPipe } from './controla-url-poster.pipe';

describe('ControlaUrlPosterPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlaUrlPosterPipe();
    expect(pipe).toBeTruthy();
  });
});
